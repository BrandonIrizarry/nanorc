


local M = require("commands")
local emit_from_file = M.emit_from_file

local filepath = arg[1]
local basename = filepath:gsub(".*/", "")
local filename = basename:match("[^.]+")

local asm_file_text = emit_from_file(filepath)

print(asm_file_text)
--assert(io.open(filename..".asm", "w"))


