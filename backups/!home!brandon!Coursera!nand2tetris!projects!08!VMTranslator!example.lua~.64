local lfs = require "lfs"



local tests = {
	"BasicLoop.vm",
	"../ProgramFlow/BasicLoop.vm",
	"NestedCall",
	"NestedCall/",
	"../FunctionCalls/NestedCall/",
	"../FunctionCalls/NestedCall"
}

local BASENAME_PATTERN = "(.*/)([^/]+)/?$"

for test in ipairs(tests) do
	print(test:match(BASENAME_PATTERN))
end
