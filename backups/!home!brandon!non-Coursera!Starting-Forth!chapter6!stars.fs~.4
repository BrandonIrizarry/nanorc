\ Problem 1
\ First create a word named STARS which will print out n stars on the same
\ line, given n on the stack:

\ Usage:
\ gforth stars.fs box.fs test-box.fs -e bye

: star ( --)
	42 emit ;

: stars ( n --)
	?dup 0= if exit then
	0 do star loop ;	
