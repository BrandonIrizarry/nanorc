

local text = [[
1
Roses are red,
violets are blue,
2
sugar is sweet,
and so are you!
3
Thus endeth the object.
]]

local function metadata ()
	local filename = ""
	local fn_name = ""
	local index = 0

	local function emit (line)
		return line:gsub("~L", filename)
	end

	local function emit_from_text ()
		filename = "poem"

		for line in text:gmatch("%w+") do
			print(emit(line))
		end
	end

	local extras = {
		

	}

	return {
		emit_from_text = emit_from_text
	}
end


metadata().emit_from_text()
